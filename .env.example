JW_USERNAME=your_username
JW_PASSWORD=your_password
CONFIG_PATH=/path/to/config_folder

# Public URL of the relay that sits between the fetcher and the
# teaching-affairs API, and the shared secret it expects.
RELAY_URL=https://your-tunnel-host:port
RELAY_SECRET=your_relay_secret